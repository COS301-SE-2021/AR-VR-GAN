syntax = "proto3";

package model;

service ModelController {
    rpc HandleCoords(RequestDto) returns (ResponseDto) {}
    rpc RunPython(RequestDto) returns (ResponsePythonDTO) {}
}

message RequestDto {
    repeated double data = 1;
}

message ResponseDto {
    double sum = 1;
}

message ResponsePythonDTO {
    string data = 1;
}