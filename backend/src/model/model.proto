syntax = "proto3";

package model;

service ModelController {
    rpc HandleCoords(stream RequestDto) returns (stream ResponseDto) {}
    rpc RunPython(stream RequestDto) returns (stream ResponsePythonDTO) {}
}

message RequestDto {
    repeated double data = 1;
}

message ResponseDto {
    bytes data = 1;
}

message ResponsePythonDTO {
    bytes data = 1;
}